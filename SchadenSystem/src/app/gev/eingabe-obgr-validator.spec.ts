import { EingabeObgrValidator } from './eingabe-obgr-validator';

describe('EingabeObgrValidator', () => {
  it('should create an instance', () => {
    expect(new EingabeObgrValidator()).toBeTruthy();
  });
});
